@page "/libros"
@rendermode InteractiveServer
@inject NavigationManager Navigation
@inject LibrosService service


<PageTitle>Index</PageTitle>
<div class="container">
    <div class="card shadow-lg">
        <div class="card-header d-flex justify-content-between aling-items-center mb-4">
            <h5 class="card-title"> Registro Libros</h5>
            <a href="/libros/create" class="btn btn-primary">
                <span class="bi bi-plus-square mt-3"></span> Crear
            </a>
        </div>
        @*inputs y botones*@
        <div class="card-body">

             <div class="row">
                 <div class="col-3">
                     <label class="col-form-label"><strong>Filtrar por</strong></label>
                 </div>

                 <div class="col-4">
                     <label class="col-form-label"><strong>B&uacute;squeda</strong></label>
                 </div>
             </div>
            <div class="row align-items-center">
                <div class="col-3">
                    <InputSelect class="form-select" @bind-Value="Filtro">
                        <option value="" selected disabled> Elija una opci&oacute;n</option>
                            <option value="Titulo">Titulo</option>
                            <option value="Autor">Autor</option>
                    </InputSelect>
                </div>
                @*Busqueda*@
                <div class="col-4">
                    <div class="input-group">
                        <input class="form-control" @bind="ValorFiltro" placeholder="Buscar" />
                        <button type="button" class="btn btn-outline-primary bi bi-search" @onclick="Buscar"></button>
                    </div>
                </div>
                @*Restablecer*@
                <div class="col-3">
                    <button type="button" class="btn btn-outline-secondary bi bi-arrow-counterclockwise" @onclick="Restablecer"></button>
                </div>
            </div>

            <table class="table table-hover text-center mt-3">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Titulo</th>
                        <th>Autor</th>
                        <th>Año</th>
                        <th>Opciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var libro in ListaLibros)
                    {
                        <tr>
                            <td>@libro.LibroId</td>
                            <td>@libro.Titulo</td>
                            <td>@libro.Autor</td>
                            <td>@libro.AnioPublicacion</td>
                            <td>
                                <a href="/libro/edit/@libro.LibroId" class="btn btn-outline-primary bi bi-pencil-square"></a>
                                <a href="/libro/delete/@libro.LibroId" class="btn btn-outline-danger bi bi-trash"></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="card-footer d-flex justify-content-between align-content-center mb-4">
            <label class="ms-3"><strong>Cantidad:</strong> @ListaLibros.Count</label>
        </div>
    </div>
</div>

@code {
    public List<Libros> ListaLibros { get; set; } = new();
    private string Filtro { get; set; } = string.Empty;
    private string ValorFiltro { get; set; } = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        ListaLibros = await service.GetList(l => true);
    }

    private async Task Buscar()
    {
        if (ValorFiltro.Trim() == "") return;

        if(Filtro == "Titulo")
        {
            ListaLibros = await service.GetList(l => l.Titulo.ToLower().Contains(ValorFiltro.ToLower()));
        }
        else if(Filtro == "Autor")
        {
            ListaLibros = await service.GetList(l => l.Autor.ToLower().Contains(ValorFiltro.ToLower()));
        }
    }

    private async Task Restablecer()
    {
        ListaLibros = await service.GetList(l => true);
        Filtro = string.Empty;
        ValorFiltro = string.Empty;
        
    }
}
